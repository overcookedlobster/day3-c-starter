#ifndef SWAP_H
#define SWAP_H

/**
 * Swaps the values pointed to by two integer pointers.
 * @param a Pointer to first integer
 * @param b Pointer to second integer
 */
void swap(int *a, int *b);

#endif

